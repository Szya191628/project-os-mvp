# Project OS 领域词汇

本文件只记录业务概念，不记录数据库、框架或接口实现细节。

## 组织

系统中的企业空间。当前组织为“感知起源科技”。组织拥有成员、部门、项目和工作日历。

## 成员

可以登录系统、被分配任务、提交交付物或查看团队负载的人。成员的外部身份（例如钉钉账号）与系统成员是两个概念，但可以建立绑定关系。一个成员可以同时属于多个部门，组织同步需要保留全部部门归属。

权限按“全局基础角色 + 项目级授权”判断：L1 是组织级全局管理员，普通成员默认是 L3；L2 只在被授权项目内生效，同一成员可以在不同项目分别具有 L2 或 L3。L3 可以查看参与项目的全部节点和交付物，但只能操作自己负责的任务。

## 项目

一个需要被计划、执行和交付的业务目标。项目拥有负责人、成员范围、状态、起止日期、预算和一条研发流程。

## 项目组合

多个相关但可独立执行的项目组成的上层管理单元。例如一条产品线可以由需求、硬件、软件、测试等多个项目组成。项目组合只负责汇总项目进度、健康度、资源和资金，不替代项目自身的流程图、任务和排期；一个项目同一时刻最多归属一个项目组合，也可以暂不分组。

## 流程

项目的任务节点和依赖关系组成的有向图。流程是项目的计划模型，不等同于某个具体执行人的当前状态。

## 流程版本

某一时刻可被保存、发布或回看的流程快照。草稿版本允许编辑；已发布版本作为任务执行和排期的依据。发布新版本不会抹掉任务已经产生的实际执行记录。我的任务只读取项目当前已发布版本：项目存在且已有发布版本就按该版本显示，发布新版本后随之更新，草稿和历史版本不影响显示。

## 节点

流程图中的开始节点、任务节点、里程碑节点或结束节点。任务节点和里程碑节点是可执行任务的载体。

## 依赖

两个节点之间的前置/后置关系。当前基线为完成到开始（FS）：后置任务的正式计时要等全部前置任务提交完成；多个前置任务取最晚完成时间。

## 任务执行

任务在已发布流程中的运行状态，包括未开始、进行中、受阻、提前结束、如期结束、超期结束和到期未完成，以及实际开始、实际完成、进度和超期原因。

## 交付物

任务完成时用于证明闭环的文件、链接或钉钉关联记录。交付物可以有多个版本，并记录版本、提交人和提交时间。

## 可开始

任务的全部前置依赖已经满足，系统允许正式开始计时。任务已发布不代表任务已经可开始。

## 工作日历

决定计划排期如何跳过周末、法定节假日、公司休息日和调休工作日的项目规则。

## 通知

由任务发布、依赖解锁、临期或超期等事件产生的站内或外部提醒。通知事件必须可追踪且幂等。

## 外部身份

成员在钉钉等外部身份提供方中的账号标识。外部身份只负责认证和映射，不替代系统成员主键。

## Agent 提案

Agent 根据项目、流程和任务数据生成的分析、排期模拟或流程草稿。提案不是流程版本，不会自动改变项目计划；需要用户在项目工作台确认后，才能保存为草稿或进入发布流程。

## 流程发布者

被授权将流程草稿转为已发布流程版本的人，通常是管理员或项目负责人。发布者需要在发布前确认节点、依赖、工作日历和任务闭环条件。
